VITE_SUPABASE_URL=
# Use the "publishable" key from Project Settings > API Keys (safe for frontend code).
# Never put the "secret"/service_role key here — that one bypasses RLS.
VITE_SUPABASE_PUBLISHABLE_KEY=
